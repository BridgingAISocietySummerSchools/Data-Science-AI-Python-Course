# Core data-science stack for the course.
# Python 3.10+ recommended.

# Jupyter environment
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.0.0

# Numerics & data manipulation
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Visualisation
matplotlib>=3.7
seaborn>=0.12          # Track 1 notebook 06 and Track 2 notebook 10
# Optional extra — no notebook requires it, but handy for your own work:
plotly>=5.0

# Machine learning
scikit-learn>=1.3

# Deep learning (Track 2, notebook 14 only — already installed in Google Colab)
torch>=2.0
